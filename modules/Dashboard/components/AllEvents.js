import styled from "styled-components";

const AllEvents = ({ className }) => {
  return (
    <main className={className}>
      All events will be shown here once you make them
    </main>
  );
};

export default styled(AllEvents)``;
