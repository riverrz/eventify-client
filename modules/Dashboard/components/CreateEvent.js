import { Formik } from 'formik';
import styled from 'styled-components';

const CreateEvent = ({ className }) => {
    return(
        <div>Make your events here</div>
    );
}

export default styled(CreateEvent)``;